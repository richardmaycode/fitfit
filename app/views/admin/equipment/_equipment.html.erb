<div class="p-4 border-indigo-200 rounded-lg border">
  <p class="text-2xl">
    <%= equipment.name %>
  </p>
  <p class='text-sm'>
    <%= equipment.description_content %>
  <p>
  <div class="flex justify-between pt-2">
    <div>
      Exercises: <%= equipment.exercises.count %>
    </div>
    <div class="flex gap-2">
      <%= link_to "Edit", edit_admin_equipment_path(equipment), class: "text-indigo-600" %>
      <%= button_to "Delete", admin_equipment_path(equipment), method: :delete, class: "text-red-600" %>
    </div>
  </div>
<div>