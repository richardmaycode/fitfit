<div class="p-4 border-indigo-200 rounded-lg border">
  <p class="font-bold text-2xl pb-4">
    <%= exercise.name %>
  </p>
  <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Nam vel massa tellus. Duis nec blandit.</p>
  <div class="py-2">
  <p class="font-bold text-sm pb-2">Muscles Worked</p>
    <div class='flex flex-wrap gap-2 h-12 content-start'>
      <% if exercise.muscles.empty? %>
        <span class="bg-indigo-100 text-indigo-800 text-xs font-medium mr-2 px-2.5 py-0.5 rounded dark:bg-indigo-900 dark:text-indigo-300">No Items Selcted</span>
      <% else %>
      <% exercise.muscles.each do |muscle| %>
        <%= render partial: 'muscle_tag', locals: {muscle: muscle} %>
      <% end %>
      <% end %>
    </div>
  <div>
  <div class="flex justify-between pt-2">
    <div>
      
    </div>
    <div class="flex gap-2">
      <%= link_to "Edit", edit_admin_exercise_path(exercise), class: "text-indigo-600" %>
      <%= button_to "Delete", admin_exercise_path(exercise), method: :delete, class: "text-red-600" %>
    </div>
  </div>
<div>