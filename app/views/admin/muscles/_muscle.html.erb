<div class="p-4 border-indigo-200 rounded-lg border">
  <p class="text-2xl">
    <%= muscle.name %>
  </p>

  <div class="flex justify-between pt-2">
    <div>
      
    </div>
    <div class="flex gap-2">
      <%= link_to "Edit", edit_admin_muscle_path(muscle), class: "text-indigo-600" %>
      <%= button_to "Delete", admin_muscle_path(muscle), method: :delete, class: "text-red-600" %>
    </div>
  </div>
<div>